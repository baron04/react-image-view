---
title: react-img-view
description: React image viewer and lightbox component — zoom, pan, pinch, rotate, fit-to-window and 1:1. Headless and composable, built for reviewing document attachments and admin image fields.
template: splash
editUrl: false
# No `hero:` block on purpose — Starlight's built-in hero renders a fixed
# title/tagline/actions arrangement, and the landing markup below is the whole
# point of this page. Declaring both would stack two hero sections.
---

import { AttachmentsDemo } from '../../components/Demo.tsx';

{/* Plain relative hrefs, not root-relative. Verified by building and
    inspecting dist/*.html: Starlight's base-path rewrite only reaches its own
    generated sidebar/pagination nav — neither a literal markdown link nor a
    component's href prop gets touched by it, so anything root-relative written
    here ships without /react-img-view. A relative path needs no rewriting at
    all: it resolves against the page's own (already correct) URL. */}

<div class="riv-landing">

<section class="riv-hero">
  <p class="riv-hero__eyebrow">MIT licensed · React 18 &amp; 19 · ~10 kB</p>

  <h1 class="riv-hero__title">An image viewer for reading, not browsing</h1>

  <p class="riv-hero__tagline">
    A composable, headless React image viewer built for reviewing document
    attachments and admin image fields — not scrolling photo feeds.
  </p>

  <div class="riv-hero__actions">
    <a class="riv-btn riv-btn--primary" href="quick-start/">Get started</a>
    <a class="riv-btn riv-btn--ghost" href="api-reference/">API reference</a>
  </div>

  <code class="riv-install">npm install react-img-view</code>
</section>

{/* The demo sits above the fold on purpose: the fastest way to judge an image
    viewer is to open one. */}
<p class="riv-demo-caption">Click any image below. This is the real component, not a video.</p>

<div class="riv-demo-panel">
  <AttachmentsDemo client:load />
</div>

<div class="riv-features">
  <div class="riv-feature">
    <h2>The toolbar stays put</h2>
    <p>
      Zoom, rotate, fit-to-window and a dedicated 1:1 control, visible the whole
      time. Reviewing a document is a task, not a scroll.
    </p>
  </div>
  <div class="riv-feature">
    <h2>Gestures that hand off</h2>
    <p>
      Pinch to zoom, drag to pan, and a drag that runs out of room pages to the
      next image mid-gesture — no stutter at the boundary.
    </p>
  </div>
  <div class="riv-feature">
    <h2>Nothing you cannot replace</h2>
    <p>
      Every part is exported, every control takes <code>asChild</code>, and state
      ships as data-attributes. Restyle it, reorder it, or rebuild the whole shell.
    </p>
  </div>
  <div class="riv-feature">
    <h2>Careful about input</h2>
    <p>
      Swipe-to-dismiss responds to touch and pen only, so a mouse drag never
      closes the viewer by accident. Full keyboard support throughout.
    </p>
  </div>
</div>

</div>
