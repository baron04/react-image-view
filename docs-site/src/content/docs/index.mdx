---
title: react-img-view
description: A headless-first React image viewer with an optional preset — zoom, pan, pinch, rotate, fit-to-window and 1:1, built for document and image review.
template: splash
editUrl: false
# No `hero:` block on purpose — Starlight's built-in hero renders a fixed
# title/tagline/actions arrangement, and the landing markup below is the whole
# point of this page. Declaring both would stack two hero sections.
---

import { AttachmentsDemo } from '../../components/Demo.tsx'

<div class="riv-landing">

<section class="riv-hero">
  <p class="riv-hero__eyebrow">MIT licensed · React 18 &amp; 19 · 11.3 kB gzipped</p>

<h1 class="riv-hero__title">A composable React image viewer built for document and image review</h1>

<p class="riv-hero__tagline">
  {
    'Start with the polished preset, or replace the entire shell. Zoom, pan, pinch, rotate, fit-to-window and 1:1 are already handled.'
  }
</p>

<div class="riv-hero__actions">
  <a class="riv-btn riv-btn--primary" href="quick-start/">
    {'Get started'}
  </a>
  <a class="riv-btn riv-btn--ghost" href="api-reference/">
    {'API reference'}
  </a>
</div>

  <code class="riv-install">npm install react-img-view</code>
</section>

<p class="riv-demo-caption">Click any image below to try it.</p>

<div class="riv-demo-panel">
  <AttachmentsDemo client:load />
</div>

<div class="riv-features">
  <div class="riv-feature">
    <h2>The toolbar stays put</h2>
    <p>
      {
        'Zoom, rotate, fit-to-window and a dedicated 1:1 control, visible the whole time. Reviewing a document is a task, not a scroll.'
      }
    </p>
  </div>
  <div class="riv-feature">
    <h2>Gestures that hand off</h2>
    <p>
      {
        'Pinch to zoom, drag to pan, and a drag that runs out of room pages to the next image mid-gesture — no stutter at the boundary.'
      }
    </p>
  </div>
  <div class="riv-feature">
    <h2>Nothing you cannot replace</h2>
    <p>
      {'Every part is exported, every control takes '}
      <code>asChild</code>
      {', and state ships as data-attributes. Restyle it, reorder it, or rebuild the whole shell.'}
    </p>
  </div>
  <div class="riv-feature">
    <h2>Careful about input</h2>
    <p>
      {
        'Swipe-to-dismiss responds to touch and pen only, so a mouse drag never closes the viewer by accident. Full keyboard support throughout.'
      }
    </p>
  </div>
</div>

</div>
